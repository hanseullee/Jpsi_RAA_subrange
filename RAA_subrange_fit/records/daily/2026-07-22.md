# 2026-07-22

## Mass-fit log-y 패널

- `pp_jpsi/mass.C`를 수정했다. 상단 `pad1`은 log-y가 활성화되어 있었지만 plot y 범위가 0에서 시작하고 있었다.
- log-y 하한은 이제 `max(1e-4, yMax * 1e-4)`이며 linear 모드는 계속 0에서 시작한다.
- 다음 명령으로 ACLiC 컴파일을 확인했다.

```bash
/opt/conda/envs/root634/bin/root -l -b -q -e 'gROOT->ProcessLine(".L /data/hwan/RAA_subrange_fit/pp_jpsi/mass.C+");'
```

입력 dataset을 사용한 시각적 출력 재생성은 아직 수행하지 않았다.
