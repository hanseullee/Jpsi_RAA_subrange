# 2026-07-24

### 날짜
2026-07-24 09:35

### 작업 맥락
pp J/psi의 forward rapidity `1.8 < |y| < 2.4` b-fraction 비교에서 current subrange 결과를 legacy 2D Fit과 동일한 pT binning으로 다시 표시했다.

### 변경 사항
- `bFraction_fwd_y1p8_pp_comparison.C`의 current subrange edge를 legacy 2D Fit과 같은 10개 differential bin으로 변경했다.
- 비교 PDF, PNG, CSV를 재생성했다.

### 결과
- current subrange와 legacy 2D Fit이 각각 동일한 10개 bin에서 읽혔다.
- ROOT ACLiC 컴파일과 plot 생성이 성공했다.
- `TColor::GetColorTransparent`의 color index 913 경고는 있었으나 출력 생성에는 영향을 주지 않았다.

### 다음 작업
1. 필요하면 HIN systematic box의 color index 경고를 정리한다.

### 날짜
2026-07-24 09:42

### 작업 맥락
current subrange와 legacy 2D Fit b-fraction 비교 그림 아래에 ratio pad를 추가했다.

### 변경 사항
- ratio를 `subrange / legacy 2D Fit`으로 정의했다.
- 동일 pT bin을 파일 edge로 매칭하고, 두 통계오차를 독립으로 가정해 전파했다.
- unity 기준선을 추가하고 PDF/PNG를 재생성했다.

### 결과
- current, legacy 및 ratio가 각각 10개 point로 생성됐다.
- ratio 범위는 `0.852–0.950`이며 렌더링을 육안 확인했다.
- ROOT color index 913 경고는 기존과 동일하게 남아 있다.

### 다음 작업
1. 전 bin에서 subrange가 낮은 원인을 분석한다.
2. ratio의 fit 간 상관관계 및 systematic uncertainty를 검토한다.


### 날짜
2026-07-24 09:56

### 작업 맥락
pp J/ψ 코드의 실제 변경분을 PbPb 대응 매크로에 이식했다.

### 변경 사항
- `pb_jpsi/mass.C`, `mc_mass.C`: `NumCPU(nCPU)`와 log-y 표시를 반영했다.
- `pb_jpsi/ctau_pr.C`, `ctau_np.C`: 공통 header와 `NumCPU(nCPU)`를 반영했다.
- `pb_jpsi/subrange_mass.C`, `subrange_ctau.C`: `kReferenceDir`를 현재 프로젝트 경로로 갱신했다.
- PbPb의 입력 파일, `cBin`, binning 및 fit model 설정은 유지했다.

### 실행한 명령
```bash
/opt/conda/envs/root634/bin/root -l -b -q -e "gROOT->ProcessLine(\".L /data/hwan/RAA_subrange_fit/pb_jpsi/<macro>.C\");"
/opt/conda/envs/root634/bin/root -l -b -q -e "gSystem->mkdir(\"/tmp/raa_pb_compile_<macro>\", true); gSystem->SetBuildDir(\"/tmp/raa_pb_compile_<macro>\", true); gROOT->ProcessLine(\".L /data/hwan/RAA_subrange_fit/pb_jpsi/<macro>.C+\");"
```

### 결과
- 6개 수정 매크로 모두 ROOT interpreter load와 `/tmp` 분리 ACLiC 컴파일이 exit code 0으로 끝났다.
- PbPb 입력 ROOT 파일을 현재 환경에서 찾지 못해 실제 fit과 출력 렌더링은 검증하지 않았다.

### 다음 작업
1. PbPb 입력 경로를 확인한 뒤 대표 bin으로 체인을 실행한다.
2. mass 및 MC mass log-y 출력과 fit status를 확인한다.


### 날짜
2026-07-24 10:06

### 작업 맥락
PbPb J/ψ 체인의 rapidity/pT binning을 pp J/ψ active list와 동일하게 정렬했다.

### 변경 사항
- `pb_jpsi/run_pp_jpsi_chain.sh`를 `1.8 < |y| < 2.4` 및 pp와 동일한 12개 pT bin으로 변경했다.
- 모든 PbPb active bin은 centrality `0–90%` (`cBin 0–180`)를 사용한다.
- 새 binning에 맞게 target-bin helper, command cookbook와 help 예시를 갱신했다.
- [[decisions/Binning_decisions]]를 추가했다.

### 실행한 명령
```bash
bash -n pb_jpsi/run_pp_jpsi_chain.sh
bash run_pp_jpsi_chain.sh --dry-run --only=mass --bin-jobs=1
```

### 결과
- shell syntax 통과
- 임시 디렉터리 dry-run: `total_bins=12`, `failed_bins=0`, `killed_jobs=0`
- 실제 ROOT fit 및 산출물 생성은 수행하지 않았다.

### 다음 작업
1. PbPb 입력 경로를 확인한다.
2. 대표 bin actual run 후 전체 12개 bin을 생성한다.

### 관련 노트
- [[notes/B_fraction]]
- [[decisions/Binning_decisions]]
- [[analysis_notes]]


### 날짜
2026-07-24 10:15

### 작업 맥락
PbPb active list에 Legacy 2D Fit centrality bin을 추가했다.

### 변경 사항
- `3 < pT < 40 GeV/c`, `1.8 < |y| < 2.4`에 `0–10%`, `10–30%`, `30–50%`, `50–90%` centrality bin을 추가했다.
- Legacy nominal `06final.sh`의 `cBin 0–20`, `20–60`, `60–100`, `100–180` edge를 재현했다.
- helper와 help 예시를 갱신했다.
- [[decisions/Binning_decisions]]를 현재 16-bin 구성으로 수정했다.

### 실행한 명령
```bash
bash -n pb_jpsi/run_pp_jpsi_chain.sh
bash run_pp_jpsi_chain.sh --dry-run --only=mass --bin-jobs=16
```

### 결과
- 전체 16-bin dry-run: `failed_bins=0`, `killed_jobs=0`
- 네 Legacy centrality 호출의 `cBin` 인자를 확인했다.
- actual fit은 수행하지 않았다.

### 다음 작업
1. 대표 centrality bin actual run
2. Legacy 결과 비교 시 `|y|` 하한 `1.6` 대 `1.8` 차이 명시

### 관련 노트
- [[notes/B_fraction]]
- [[decisions/Binning_decisions]]
- [[analysis_notes]]


### 오늘 수행한 작업
- pp-matched PbPb pT bin 12개 전체 fit chain 실행.

### 주요 결과
- 12개 bin 모두 rc=0 완료. 대상은 |y|=1.8–2.4, centrality 0–90%이며 Legacy centrality 전용 4개는 제외했다.

### 미검증 사항
- fit quality와 yield의 정량 검토는 아직 하지 않았다.

### 다음 작업
- 대표 pT bin ROOT output의 fit status와 결과 요약을 확인한다.
