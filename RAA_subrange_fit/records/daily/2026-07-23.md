# 2026-07-23

## Mass-fit Y축 범위

- `pp_jpsi/mass.C` 660번 줄 부근을 수정해 표시 중인 `RooHist`에서 log-y 범위를 계산하도록 했다. 제거한 블록은 정의되지 않은 `ws`, `dsAB`, `myPlot_A` 객체를 참조하고 있었다.
- 양의 데이터 점이 하한 scale을 정하고, 가장 큰 데이터 점과 상단 error가 상한 scale을 정한다. 로그 공간 여백은 데이터 아래 10%, 위 40%이며 linear 모드는 `0.0`~`1.65*yMax`를 유지한다.

## 검증

```bash
/opt/conda/envs/root634/bin/root -l -b -q -e "gROOT->ProcessLine(\".L /data/hwan/RAA_subrange_fit/pp_jpsi/mass.C+\");"
```

ACLiC 컴파일을 성공적으로 완료했다. 입력 dataset을 사용한 plot 재생성은 수행하지 않았다.

## 기록 구조 정리

- 작업 기록 전용 경로를 `records/`로 통일했다. 일일 기록은 `records/daily/`, 개발 기록은 `records/dev_log.md`, 인계 기록은 `records/handoff.md`에 저장한다.
- 기존 두 일일 기록을 새 경로로 보존해 옮겼고, 제목과 서술을 한국어로 정리했다.
- `AGENTS.md`와 `CLAUDE.md`를 갱신해 이후 세 종류의 기록은 제목과 서술을 모두 한국어로 작성하도록 했다.

## 확인

- `rg` 검색으로 기존 `daily/`, `dev_log.md`, `handoff.md` 경로를 참조하는 Markdown 문서가 남아 있지 않음을 확인했다.
- 코드와 분석 결과 파일은 변경하지 않았다.

## Forward pp J/ψ b-fraction 비교

### 수행한 작업

- `/data/hwan/psi2S_RAA_PbPb2018/Macros/pp_Jpsi/`의 `1.8 < |y| < 2.4` 2D-fit ROOT 결과를 찾았다.
- 기존 `bFraction_fwd_pp.C`와 HEPData JSON에서 BPH-10-002 및 HIN-16-025 pp 값을 확인했다.
- 현재 subrange, 기존 2D fit, BPH, HIN을 함께 그리는 매크로와 수치 CSV를 추가했다.

### 변경 파일

- `pp_jpsi/drawing/b_frac/bFraction_fwd_y1p8_pp_comparison.C`
- `analysis_notes.md`
- `todo.md`
- `notes/B_fraction.md`
- `00_Project_Index.md`
- `records/dev_log.md`
- `records/daily/2026-07-23.md`
- `records/handoff.md`

### 실행 명령

```bash
/opt/conda/envs/root634/bin/root -l -b -q 'pp_jpsi/drawing/b_frac/bFraction_fwd_y1p8_pp_comparison.C+'
```

### 주요 결과

- 출력: `bFraction_pp_fwd_y1p8_comparison.pdf`, `.png`, `_values.csv`
- 현재 `3–4.5 GeV/c`: `0.1528189 ± 0.0021446 (stat.)`
- 기존 2D fit 같은 bin: `0.1501129 ± 0.0004970 (stat.)`
- 통계오차 기반 차이: `1.23σ`
- 현재 ctau fit p-value: `3.67×10^-103`; 검증 전 중간 결과로 취급

### 문제 및 미검증 사항

- 현재 계획한 8개 pT bin 중 7개 subrange ROOT가 없다.
- `subrange_mass.C`의 stale `kReferenceDir` 때문에 병렬 slice 로그 생성이 실패했다.
- 체인 summary는 내부 실패에도 `failed_bins=0`으로 기록했다.
- 현재 및 기존 2D fit systematic uncertainty는 아직 포함하지 않았다.

### 다음 작업

1. 경로와 exit-status 전달을 수정한다.
2. 한 bin 재실행으로 검증한 뒤 전체 누락 bin을 생성한다.
3. fit 품질과 systematic을 반영해 비교 그림을 갱신한다.

### 관련 노트

- [[notes/B_fraction]]
- [[analysis_notes]]
- [[todo]]


## Current subrange 결과 반영

- `1.8 < |y| < 2.4`의 8개 differential subrange ROOT가 생성됐음을 확인했다.
- 비교 매크로에서 `current.empty()` 조건문을 제거해 current graph와 범례를 항상 그리도록 했다.
- ROOT 컴파일과 PDF/PNG/CSV 재생성을 완료했다.

### 2026-07-23 14:46

- `pp_jpsi/run_pp_jpsi_chain.sh`의 1.8 < |y| < 2.4 pT binning을 legacy 2D Fit과 동일하게 세분화했다: 3.0-4.5, 4.5-5.5, 5.5-6.5, 6.5-7.5, 7.5-8.5, 8.5-9.5, 9.5-11.0, 11.0-15.0, 15.0-20.0, 20.0-40.0 GeV/c; integrated 3.0-6.5 및 6.5-40.0 GeV/c 유지.
- `bash -n pp_jpsi/run_pp_jpsi_chain.sh` 통과. 체인 실행은 아직 수행하지 않음.
